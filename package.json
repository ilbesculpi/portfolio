{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "homepage": "http://ilbesculpi.github.io/portfolio",
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "5.0.1"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build&&gh-pages -d build"
  }
}
